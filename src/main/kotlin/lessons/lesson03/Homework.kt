package org.example.lessons.lesson03

class Homework {

    // 1. Название мероприятия (неизменяемая публичная переменная известная на момент написания)
    val name: String = "Hackathon Survival"

    // 2. Дата проведения (публичная переменная известная на момент написания, но может быть изменена из-за переноса)
    var date: String = "17.09.2025"

    // 3. Место проведения (публичная переменная известная на момент написания, но может быть изменена)
    var location: String = "San Diego"

    // 4. Подробный бюджет мероприятия... (приватная неизменяемая переменная)
    private val budget: String = "подробная информация с перечислением поставщиков и сумм для внутреннего пользования"

    // 5. Количество участников (публичная изменяемая переменная, может меняться, так как кто-то может не прийти)
    var guests: Int = 50

    // 6. Длительность хакатона (публичная неизменяемая переменная)
    val length: Int = 3

    // 7. Контактная информация и условия соглашений... (приватная изменяемая переменная, данные для ограниченного пользования)
    private var contactInfo: String = "My providers: Provider list"

    // 8. Текущее состояние хакатона (статус) (публичная изменяемая переменная, известно всем)
    var status: String = "planned"

    // 9. Список спонсоров (публичная изменяемая переменная, они могут меняться)
    var sponsors: String = "Jim, John, Microsoft"

    // 10. Бюджет мероприятия (публичная изменяемая переменная — общий, опубликован в прессе)
    var totalBudget: String = "Около 5000 USD"

    // 11. Текущий уровень доступа к интернету (публичная изменяемая переменная, известно всем)
    var accessSpeed: String = "high"

    // 12. Информация о транспортировке/координации... (приватная изменяемая переменная)
    private var logistics: String =
        "Flight tickets are on us. Debora D. coordinates everything, see the attachments"

    companion object {
        // 13. Количество команд (публичная неизменяемая переменная, неизменный план)
        const val participants: Int = 28
    }

    // 14. Перечень задач (приватная изменяемая переменная, задачи могут меняться)
    private var taskList: String = "Снять офисное здание; подготовить мерч; распечатать бейджи"

    // 15. План эвакуации (публичная неизменяемая; подгружается по требованию, потому что большая)
    val evacuationPlan: String by lazy { "План эвакуации размещён у входа, на стенде безопасности, в нашей брошюрке, много-много страниц" }

    // 16. Список доступного оборудования (публичная изменяемая переменная)
    var equipment: String = "удлинители, проектор, колонки, микрофон, кулеры"

    // 17. Список свободного оборудования (публичная изменяемая переменная)
    var freeEquipment: String = "удлинители, микрофон"

    // 18. График питания участников (публичная изменяемая переменная с отложенной инициализацией, потому что сразу это неважно)
    lateinit var mealBreaksPlan: String

    // 19. План мероприятий на случай сбоев (приватная неизменяемая; ленивая инифиализация, потому что дорого ее всю сразу подкгужать)
    private val emergencyPlan: String by lazy { "резервный генератор; запасной роутер; запасной вариант офисного здания, список из 100 пунктов" }

    // 20. Список экспертов и жюри (публичная изменяемая переменная, опубликовано в пресс-релизе)
    var juryList: String = "Mr.X, Mr.Y, Dr. Smith, Weiss W."

    // 21. Политика конфиденциальности (публичная неизменяемая; ленивая инициализация, потому что подгружается при первом доступе. Val потому что ее раз написали юристы 5 лет назад и она не меняется)
    val privacyPolicy: String by lazy { "Публичная политика конфиденциальности версии 1.0  88 страниц" }

    // 22. Приватные отзывы (фидбэк)... (приватная изменяемая переменная. они будут получены потом )
    private lateinit var privateFeedback: String

    // 23. Текущая температура в помещении (публичная изменяемая; может быть неизвестна до замера)
    var roomTemperature: String? = null

    // 24. Данные мониторинга сети... (публичная изменяемая; может быть неизвестно до сбора)
    var networkDiagnostics: String? = null

    // 25. Регулятор скорости интернета (публичная изменяемая переменная, ее можно менять, она известна сразу)
    var bandwidthThrottle: String = "0.5"

    // 26. Уровень освещения (публичная изменяемая, меняется, известна сразу)
    var lightLevel: String = "средний"

    // 27. Лог событий мероприятия (приватная изменяемая, он редактируется, и хоть он и большай но не может быть lateinit потому что редактируемый)
    private var eventLog: String = "инициализация системы логирования"

    // 28. Возможность получения медпомощи (да/нет) (публичная неизменяемая булево, предполагается, что о медпомощи орги договорились заранее и она во время мероприятия никуда не может деться)
    val medicalAssistanceAvailable: Boolean = true

    // 29. Планы и процедуры безопасности (приватная неизменяемая; by lazy потому что читаем только когда потребуется, val потому что поступило от юристов и не меняется)
    private val safetyProcedures: String by lazy { "инструкции по ЧС; точки сбора; контакты ответственных" }

    // 30. Регистрационный номер мероприятия (публичная неизменяемая, назначен раз и навсегда)
    val registrationNumber: String = "HK-2025-00123"


    // 31. Максимально допустимый уровень шума (публичная неизменяемая переменная val, это определено законодательством и нельзя изменить)
    val maxNoiseLevel: Int = 85

    // 32. Индикатор превышения уровня шума (публичная изменяемая; может быть неизвестно до измерения)
    var isNoiseExceeded: Boolean? = null

    // 33. План взаимодействия с прессой (публичная изменяемая переменная)
    var pressPlan: String = "пресс-релиз; брифинг в 12:00; доступ в пресс-зону"

    // 34. Детальная публичная информация о проектах каждой команды, сбор данных включает в себя компиляцию кода и сбор статистики прогона автоматизированных проверок. (публичная изменяемая lateinit, все данные появятся позже, их очень много)
    lateinit var publicProjectDetails: String

    // 35. Статус получения разрешений (приватная изменяемая переменная, для ограниченного доступа)
    private var permitsStatus: String = "в процессе"

    // 36. Доступность специализированного оборудования (публичная изменяемая; может быть неизвестна до проверки)
    var specialToolsAvailable: Boolean? = null

    // 37. Список партнеров мероприятия (публичная изменяемая, опубликовано на сайте)
    var partners: String = "Company A; Company B; Local Hubs"

    // 38. Отчёт (фото, видео, отзывы) — после завершения (публичная изменяемая с отложенной инициализацией)
    lateinit var report: String

    // 39. Внутренние правила распределения призов (приватная неизменяемая; by lazy потому что они нужны только по запросу, и они объемные)
    private val awardRulesInternal: String by lazy { "методика оценки; порядок распределения; тайбрейки" }

    // 40. Список разыгрываемых призов (публичная изменяемая, призы известны сразу)
    var prizeList: String = "ноутбук; колонки; сертификаты"

    // 41. Контакты экстренных служб и медперсонала (приватная неизменяемая переменная, известно сразу и не может измениться по ходу)
    private val emergencyContacts: String =
        "112; адрес ближайшей клиники: Ленина 46; координатор: Иванова  +8 600 000 000"

    // 42. Особые условия доступности (публичная изменяемая переменная, список известен сразу и может пополняться, если нужно)
    var accessibilityProvisions: String = "пандусы; места у сцены; сурдоперевод при необходимости"

    // 43. Общее настроение участников (публичная изменяемая lateinit потому что мы его узнаем потом)
    lateinit var participantsMood: String

    // 44. Подробный план хакатона (приватная изменяемая с отложенной инициализацией потому что участникам он будет нужен только после начала)
    private lateinit var detailedPlan: String

    // 45. Имя специального гостя (публичная изменяемая переменная на случай замены гостя)
    var specialGuestName: String = "Zuckerberg"

    // 46. Вместимость площадки (публичная неизменяемая и хотелось бы сюда воткнуть const но компаньон должен быть только один в классе а вне класса выносить это нарушится наш красивый список)
    val venueCapacity: Int = 120

    // 47. Часы на команду (публичная неизменяемая переменная, расписание сторогое и меняться не может)
    val hoursPerTeam: Int = 24


}